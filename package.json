{
  "scripts": {
    
    "postinstall": "cd client && npm install",
    "heroku-postbuild": "cd client && npm run heroku-postbuild"
    
  },
  "engines": {
    "node": "14.*.*",
    "npm": "6.*.*"
  },
  "cacheDirectories": [
    "client/node_modules",
    ".cache/Cypress"
  ],
  "dependencies": {
  }
}
